import LayoutDefault from '@/layouts/LayoutDefault';
import type {NextPage} from 'next';

const Home: NextPage = () => {
    return (
        <LayoutDefault>
            IndexPage
        </LayoutDefault>
    );
};

export default Home;
